/**
 * Check that input and output of unmodified HTML are the same
 */
var test = require('tape');
var mkSrc = require('../index.js');
var fs = require('fs-extra');

test('parse and output an HTML file', function (t) {

  t.plan(1);

  var file = "./test/data/default.html"

  // TODO not done oops

});
